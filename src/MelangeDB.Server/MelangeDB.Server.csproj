<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Description>WebSocket transport, subscription engine, and identity/auth integration. Hosted inside the developer's own ASP.NET Core app.</Description>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="10.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\MelangeDB.Core\MelangeDB.Core.csproj" />
    <ProjectReference Include="..\MelangeDB.Protocol\MelangeDB.Protocol.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="MelangeDB.Transport.Tests" />
    <!-- The cluster package composes per-shard transports and the gateway from the same
         connection, authenticator, and subscription machinery a single node uses. -->
    <InternalsVisibleTo Include="MelangeDB.Cluster" />
  </ItemGroup>

</Project>

<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Description>Optional PostgreSQL projection for relational-tier tables.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\MelangeDB.Core\MelangeDB.Core.csproj" />
    <!-- For the Postgres-backed IMembershipStore: cluster membership lives in the hub's own
         Postgres rather than a new consensus dependency (docs/road-to-0.1/plan-phase-09.md, settled). -->
    <ProjectReference Include="..\MelangeDB.Cluster\MelangeDB.Cluster.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Npgsql" Version="9.0.3" />
  </ItemGroup>

</Project>
